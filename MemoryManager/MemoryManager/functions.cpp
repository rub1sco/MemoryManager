#include <stdio.h>
#include "functions.h"

//provides random look up value
int randomNumberGenerator(){
    int randVal = 0;
    
    return randVal;
}

std::tuple<int,int> parser(int logicalAddress){
    int pageNumber = 0;
    int offset = 0;
    
    return std::make_tuple(pageNumber,offset);
}

void replacePage(std::tuple<int,int> parsedData){
    
}
